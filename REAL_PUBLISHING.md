# النشر الحقيقي على صفحات فيسبوك

يستخدم زر **نشر فعلي على الصفحة** واجهة Meta Pages API الرسمية، وليس نقرات داخل WebView. لا يعتبر التطبيق الطلب ناجحاً إلا بعد أن تعيد Meta استجابة ناجحة وبها معرّف للكائن المنشور، ثم يعرض التطبيق المعرّف في سجل الحملة.

## تسجيل الدخول المبسط

افتح تبويب **الحساب** واضغط **تسجيل الدخول عبر فيسبوك**. يفتح التطبيق متصفحاً داخلياً كما في النسخة السابقة، ويمكنك إدخال بيانات ملفك الشخصي داخل صفحة فيسبوك. بعد إتمام الدخول اضغط **تم — حفظ جلسة الدخول**؛ يحفظ التطبيق جلسة المتصفح محلياً ويعرض حالة الجلسة ومعرّف `c_user` إن توفر. لم يعد إدخال الكوكيز يدوياً مطلوباً في الواجهة، ويظل زر تسجيل الخروج متاحاً لمسح الجلسة وتبديل الحساب.

تسجيل الدخول الداخلي مخصص لحفظ جلسة المستخدم والمعاينة اليدوية. **لا تستخدم Meta تسجيل دخول الملف الشخصي وحده كصلاحية رسمية للنشر البرمجي على صفحة**؛ لذلك يحتاج مسار النشر المؤكد أيضاً إلى إعداد الصفحة أدناه.

## المتطلبات الرسمية للنشر

يجب إنشاء تطبيق في [Meta for Developers](https://developers.facebook.com/) وتفعيل مسار Meta المناسب. يحتاج المستخدم الذي يدير الصفحة إلى الصلاحيات التي تتطلبها Meta في إصدار Graph API المستخدم، وأهمها عادةً `pages_manage_posts`، إضافة إلى صلاحيات قراءة/إدارة الصفحة عند الحاجة. للاستخدام مع أشخاص أو صفحات خارج وضع التطوير قد تطلب Meta إكمال App Review وAdvanced Access أو Business Verification.

يجب أن يكون للمستخدم على الصفحة دور يسمح بمهمة `CREATE_CONTENT` أو `MANAGE`. استخرج **Page ID** و**Page access token** عبر المسار الرسمي في Meta. لا تضع رمز الوصول داخل مستودع GitHub ولا تشاركه في رسائل أو لقطات شاشة.

## الإعداد داخل التطبيق

من تبويب **الحساب**، أدخل معرّف الصفحة الرقمي في حقل **Page ID** ورمز **Page access token** في الحقل المخصص. اضغط **تحقق من اتصال الصفحة**، ثم راجع اسم الصفحة وحالة الاتصال. لا يبدأ النشر إذا كانت البيانات ناقصة أو إذا رفضت Meta التحقق منها.

## نشر النص والصور والفيديو

من تبويب **الحملات** اكتب النص، ثم اضغط **اختيار من المعرض**. يدعم المسار الرسمي الحالات التالية:

| نوع المحتوى | ما يدعمه التطبيق |
|---|---|
| نص فقط | منشور نصي على الصفحة عبر `/feed` |
| صورة واحدة مع نص | رفع الصورة إلى `/photos` مع `caption` والتحقق من `post_id` |
| عدة صور مع نص | رفع الصور أولاً كوسائط غير منشورة، ثم إنشاء منشور `/feed` باستخدام `attached_media` |
| فيديو مع نص | فيديو واحد مع `description` إلى مسار `/videos` والتحقق من معرّف الفيديو |
| صور وفيديو معاً | غير مسموح؛ يمنع التطبيق الإرسال قبل الطلب برسالة واضحة |

تظهر **معاينة داخل التطبيق** للملفات المختارة، بما في ذلك مصغرات الصور واسم الفيديو. يمكن مسح الوسائط وإعادة اختيارها قبل النشر. لا تحتاج الحملة إلى وجود مجموعات؛ تبويب المجموعات يحفظ الروابط فقط ولا يعلن نجاح النشر داخلها.

اضغط **نشر فعلي على الصفحة**. عند النجاح يظهر معرّف أعادته Meta في سجل الحملة وتزداد خانة المنشورات الناجحة. عند الفشل يعرض التطبيق رسالة Meta ولا يزيد عداد النجاح. رفع الفيديو قد يستغرق وقتاً أطول من النص والصور، ولذلك يستخدم التطبيق مهلة رفع موسعة ورسالة خطأ منفصلة عند انتهاء الاتصال.

## اختبار قبول آمن

ابدأ بصفحة اختبار تملكها وبصورة أو فيديو قصير غير حساس. سجّل الدخول من المتصفح الداخلي، أتمم إعداد Page ID وPage access token، اضغط التحقق، ثم جرّب نصاً قصيراً، وبعده صورة، ثم فيديو واحد. راجع معرّف العملية في سجل الحملة وظهور المحتوى فعلياً على الصفحة نفسها. لا يمكن تأكيد منشور حقيقي من بيئة البناء وحدها دون بيانات Meta صالحة ومصرح بها من مالك الصفحة؛ لذلك يجب تنفيذ اختبار القبول النهائي على جهازك وبحسابك.

## حدود وأمان الإنتاج

لا يعلن التطبيق أن النشر داخل المجموعات تم فعلياً؛ مسار DOM/WebView ليس دليلاً موثوقاً ولا يمثل واجهة Meta API رسمية. كما أن حفظ Page access token محلياً مناسب لتطبيق شخصي/داخلي فقط. إذا كان التطبيق سيوزع على مستخدمين متعددين، فالأفضل نقل استدعاءات Graph API إلى خادم آمن، أو استخدام تخزين مشفر وتدوير للرموز، وعدم إرسال Page access token إلى عميل Android مباشرة.
