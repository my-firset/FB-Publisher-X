# النشر الحقيقي على صفحات فيسبوك

أصبح زر **نشر فعلي على الصفحة** في التطبيق يستخدم واجهة Meta Pages API الرسمية، وليس نقرات داخل WebView. لا يعتبر التطبيق الطلب ناجحاً إلا إذا أعادت Meta استجابة HTTP ناجحة وبها `id` للمنشور، ثم يعرض التطبيق هذا المعرّف في سجل الحملة.

## المتطلبات

يجب إنشاء تطبيق في [Meta for Developers](https://developers.facebook.com/) وإضافة Facebook Login for Business أو المسار الذي توفره Meta في لوحة التطبيق. يحتاج المستخدم الذي يدير الصفحة إلى صلاحيات مناسبة، أهمها `pages_manage_posts` و`pages_show_list`، وقد تحتاج الصلاحيات الأخرى مثل `pages_read_engagement` و`pages_manage_metadata` بحسب طريقة استخراج الصفحات والوظائف المطلوبة. للاستخدام مع أشخاص أو صفحات خارج وضع التطوير، يجب إكمال متطلبات Meta App Review وBusiness Verification أو Advanced Access عندما تطلبها Meta.

يجب أن يكون للمستخدم على الصفحة دور يسمح بمهمة `CREATE_CONTENT` أو `MANAGE`. استخدم مسار Meta الرسمي للحصول على **Page ID** و**Page access token**، أو استخرج الصفحات من endpoint الحساب الذي توفره Meta للمستخدم المصرح له. لا تضع رمز الوصول داخل مستودع GitHub ولا تشاركه في رسائل أو لقطات شاشة.

## الإعداد داخل التطبيق

افتح تبويب **الحساب**، ثم أدخل معرّف الصفحة الرقمي في حقل **Page ID** ورمز **Page access token** في الحقل المخصص. اضغط **تحقق من اتصال الصفحة**. لا يبدأ النشر إذا كانت البيانات ناقصة أو إذا رفضت Meta التحقق منها.

بعد نجاح التحقق افتح تبويب **الحملات**، اكتب نصاً، وأزل أي ملفات مرفقة لأن المسار الحالي يضمن النشر النصي فقط. اضغط **نشر فعلي على الصفحة**. عند النجاح يظهر **Post ID** في سجل الحملة وتزداد خانة المنشورات الناجحة. عند الفشل يعرض التطبيق رسالة Meta ولا يزيد عداد النجاح.

## حدود مقصودة

واجهة المجموعات في التطبيق أصبحت لإدارة وحفظ الروابط فقط. لا يعلن التطبيق أن النشر داخل المجموعات تم فعلياً، لأن مسار DOM/WebView ليس دليلاً على نجاح النشر ولا يمثل واجهة Meta API رسمية. كذلك لا يدعم المسار الحالي رفع الصور أو الفيديو من الجهاز؛ يمنع التطبيق بدء الطلب عند وجود مرفقات كي لا يعطي نتيجة مضللة.

## اختبار آمن

ابدأ بصفحة اختبار تملكها، وبمنشور نصي غير حساس. اختبر زر التحقق أولاً، ثم راجع Post ID في سجل التطبيق وصفحة فيسبوك نفسها. لا يمكن تأكيد منشور حقيقي من بيئة البناء وحدها من دون Page ID ورمز وصول صالحين ومصرح بهما من مالك الصفحة؛ لذلك يجب إجراء اختبار القبول النهائي على جهازك وببيانات Meta الخاصة بك.

## ملاحظات أمان الإنتاج

يحفظ التطبيق رمز الصفحة محلياً في تفضيلات التطبيق كي يعمل كتطبيق شخصي/داخلي. إذا كان التطبيق سيُوزع على مستخدمين متعددين، فالخيار الأفضل هو نقل استدعاءات Graph API إلى خادم آمن أو تخزين مشفر وإضافة تدوير للرموز، وعدم إرسال Page access token إلى عميل Android مباشرة.
